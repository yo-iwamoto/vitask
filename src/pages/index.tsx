import type { NextPage } from 'next';
import { Button, Typography } from '@mui/material';

const Page: NextPage = () => {
  return (
    <>
      <Typography>すごい課題通知</Typography>
      <Button variant="contained">はじめる</Button>
    </>
  );
};

export default Page;
